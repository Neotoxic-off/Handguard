<UserControl x:Class="Handguard.Client.Views.UploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Handguard.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid>
        <Button Style="{StaticResource BorderButton}" BorderThickness="0" Height="80" Width="80" Command="{Binding UploadCommand}">
            <Path Data="{StaticResource Upload}" Fill="White" Height="50" Width="50" Stretch="Uniform"/>
        </Button>
    </Grid>
</UserControl>
